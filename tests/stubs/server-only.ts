/**
 * Test stub for the `server-only` package.
 *
 * In a Next build, importing `server-only` from a client component is a build
 * error — which is exactly the guarantee we want in production. Under Vitest
 * there is no client/server split, so it is replaced with an empty module.
 */
export {};
